# PamirNet FreeRADIUS client configuration.
# Registered MikroTik clients are generated into the shared pamirnet.conf file.

client localhost {
    ipaddr = 127.0.0.1
    secret = local-development-only
    nas_type = other
}

$INCLUDE clients.d/pamirnet/pamirnet.conf
