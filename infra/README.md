# Infrastructure

Phase 0 uses the root `docker-compose.yml` for local development.

Later phases will add:

- production Docker Compose configuration
- Nginx/reverse proxy configuration
- WireGuard onboarding and server configuration
- FreeRADIUS network exposure rules
- backup/restore scripts
- deployment and operational runbooks

Kubernetes is intentionally out of scope for the initial deployment.
